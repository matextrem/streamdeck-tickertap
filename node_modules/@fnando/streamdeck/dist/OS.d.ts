export type Platform = "mac" | "windows";
export declare class OS {
    platform: Platform;
    minimumVersion: string;
    constructor(params: {
        platform: Platform;
        minimumVersion: string;
    });
    toManifest(): {
        Platform: Platform;
        MinimumVersion: string;
    };
}
